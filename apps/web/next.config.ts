import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;

//for redirect if reqd : - 
// import type { NextConfig } from "next";

// const nextConfig: NextConfig = {
//   async redirects() {
//     return [{ source: "/home", destination: "/dashboard", permanent: true }];
//   },
// };

// export default nextConfig;
